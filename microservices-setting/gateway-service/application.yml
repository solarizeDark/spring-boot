spring:
  application:
    name: gateway

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}
    hostname: localhost

server:
  port: 80